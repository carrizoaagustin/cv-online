meta {
  name: find user resources
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users/:userId/resources?format=[jpeg,pdf,...]
  body: none
  auth: none
}

params:query {
  format: [jpeg,pdf,...]
}

params:path {
  userId: 
}

docs {
  Se puede filtrar por formato usando un array con los formatos deseados.
  
}
